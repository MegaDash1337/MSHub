export default class Track {
    constructor(name, author, cover, source, type) {
        this.name = name;
        this.author = author;
        this.cover = cover;
        this.source = source;
        this.type = type;
    }
}